use client; import { useState, useEffect, useRef } from \"react\"; import { Volume2, VolumeX, Play, Pause } from \"lucide-react\"; const MusicPlayer = ({ audioSrc }) => { const [isPlaying, setIsPlaying] = useState(false); const [isMuted, setIsMuted] = useState(false); const audioRef = useRef(null); useEffect(() => { audioRef.current = new Audio(audioSrc); audioRef.current.loop = true; return () => { if (audioRef.current) { audioRef.current.pause(); } }; }, [audioSrc]); const togglePlay = () => { if (audioRef.current) { if (isPlaying) { audioRef.current.pause(); } else { audioRef.current.play(); } setIsPlaying(!isPlaying); } }; const toggleMute = () => { if (audioRef.current) { audioRef.current.muted = !isMuted; setIsMuted(!isMuted); } }; return ( <div style={{ position: \"fixed\", bottom: \"1rem\", right: \"1rem\", background: \"rgba(0,0,0,0.5)\", padding: \"0.75rem\", borderRadius: \"9999px\", display: \"flex\", gap: \"0.5rem\", zIndex: 50 }}> <button onClick={togglePlay} style={{ color: \"white\" }}>{isPlaying ? \"⏸\" : \"▶\"}</button> <button onClick={toggleMute} style={{ color: \"white\" }}>{isMuted ? \"🔇\" : \"🔊\"}</button> </div> ); }; export default MusicPlayer;
